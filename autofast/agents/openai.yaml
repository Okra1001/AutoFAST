interface:
  display_name: "AutoFAST v1.0"
  short_description: "Universal, traceable OpenFAST simulation workflows"
  default_prompt: "Use $autofast to inspect this OpenFAST setup and take it to a traceable, validated simulation result."
policy:
  allow_implicit_invocation: true
